<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limin.mapper.UserMapper">

    <select id="queryUserList" resultType="User">
        select * from user
    </select>

    <select id="queryUserName" parameterType="String" resultType="User">
        select * from user where username = #{username}
    </select>

    <insert id="createUser" parameterType="User">
        insert into user (id,username,password,userRole,createDate) values (#{id},#{username},#{password},#{userRole},#{createDate})
    </insert>

    <update id="updateUser" parameterType="User">
        update user set username=#{username},password=#{password},userRole=#{userRole},createDate=#{createDate} where id = #{id}
    </update>

    <delete id="deleteUserById" parameterType="String">
        delete from user where id = #{id}
    </delete>

</mapper>
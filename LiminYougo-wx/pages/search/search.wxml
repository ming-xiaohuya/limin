<!--pages/search/search.wxml-->
<SearchBar></SearchBar>
<!-- <view class="search-row">
    <input value="{{inpValue}}" type="text" placeholder="请输入关键字" bindinput = "handleInput"></input>
    <button hidden="{{!isFocus}}" bindtap = "handleCancel">取消</button>
</view>
<view class="search-content">
    <navigator 
        class="search-item"
        wx:for="{{secarchList}}"
        wx:key="id"
        url="/pages/product_detail/index?pid={{item.id}}"
    >
        {{item.title}}
    </navigator>
</view> -->
